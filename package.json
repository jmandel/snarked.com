{
  "name": "snarked.com",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "serve": "bun run build.ts && cd dist && python3 -m http.server 8000",
    "dev": "bun run build.ts && cd dist && python3 -m http.server 8000"
  },
  "dependencies": {
    "marked": "^14.1.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
